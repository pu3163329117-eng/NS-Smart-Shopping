# Railway专用Dockerfile
# 只构建前端，后端和数据库由Railway单独服务提供

FROM node:18-alpine

WORKDIR /app

# 复制前端代码
COPY smart-ja-web/package*.json ./
COPY smart-ja-web/ ./

# 安装依赖
RUN npm install

# 构建前端
RUN npm run build

# 暴露端口
EXPOSE 3002

# 启动前端服务
CMD ["npm", "run", "preview"]